<!--pages/my_meeting_list/my_meeting_list.wxml-->
<!-- <view class="page" data-weui-theme="{{theme}}">
    <view class="page__bd">
            <view class="heng  all_gray">
                <view class="my_meeting_list"  wx:for="{{myMeetingList}}" wx:for-item="meeting" wx:for-index="index" wx:key="id">
                    <view bindtap="goToMeetingDetail" data-item="{{meeting.meetingId}}">
                        <view class="weui-cells weui-cells_after-title yuanjiao">
                            <view hidden="true">房间id:{{meeting.roomId}}</view>
                            <view hidden="true">会议id:{{meeting.meetingId}}</view>
                            <view >日期：{{meeting.dateTimeRange}}</view>
                            <view >时间：{{meeting.dateTimeRange}}</view>
                            <view >会议室: {{meeting.roomName}}</view>
                            <view >部门: {{meeting.departmentName}}</view>
                            <view >主题: {{meeting.meetingName}}</view>
                    </view>
                </view>
            </view>
        </view>
    </view>
</view> -->

<view class="all_gray">
    <view class="my_meeting_list"  wx:for="{{myMeetingList}}" wx:for-item="meeting" wx:for-index="index" wx:key="id">
        <view bindtap="goToMeetingDetail" data-item="{{meeting.meetingId}}">
            <view class="weui-cells weui-cells_after-title">
                <view hidden="true">房间id:{{meeting.roomId}}</view>
                <view hidden="true">会议id:{{meeting.meetingId}}</view>
                <view >日期：{{meeting.dateTimeRange}}</view>
                <view >时间：{{meeting.dateTimeRange}}</view>
                <view >会议室: {{meeting.roomName}}</view>
                <view >部门: {{meeting.departmentName}}</view>
                <view >主题: {{meeting.meetingName}}</view>
            </view>
        </view>
    </view>
</view>



